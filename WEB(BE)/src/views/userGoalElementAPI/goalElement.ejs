<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Goal Element</title>
</head>
<body>
    <%  if (goalElement) { %>
        <h1>Goal Element: ID [<a href="/userGoalElementAPI/ctfInfo/<%=goalElement._id %>"><%=goalElement._id %></a>]</h1>
        <h2>현재 <%=username %> 님의 Goal: <%=goalElement.name %></h2>
    <% } %>

    <form action="/userGoalElementAPI/goal/<%=username %>" method="post">
        <label for="goalElement">새로운 Goal을 정해 보세요!</label>
        <select name="goalElement" id="goalElement">
            <option value="">Options</option>
            <% for (var ctfInfo of ctfInfoList) { %>
                <option value="<%=ctfInfo.name %>"><%=ctfInfo.name %></option>
            <% } %>
        </select>
        <button>Submit</button>
    </form>
    <form action="/userGoalElementAPI/goal/<%=username%>?_method=DELETE" method="POST">
        <button>DELETE</button>
    </form>
</body>
</html>