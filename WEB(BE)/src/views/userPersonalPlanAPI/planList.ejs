<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plan List</title>
</head>
<body>
    <h1><%=username %> 의 Plan List</h1>

    <ul>
        <% for(var i = 0; i < list.length; i++) { %>
            <li>
                <a href="/userPersonalPlanAPI/<%=username %>/plans/<%=list[i] %>"><%=list[i] %></a>
            </li>
        <% } %>
    </ul>

    <form action="/userPersonalPlanAPI/<%=username %>/plans" method="POST" >
        <div>
            <label for="detail">detail</label>
            <input type="text" name="detail" id="detail" required>
        </div>
        <div>
            <span>Steady</span>
            <label><input type="radio" name="steady" value="true"> True</label>
            <label><input type="radio" name="steady" value="false"> False</label>
        </div>
        <button>Submit</button>
    </form>
</body>
</html>